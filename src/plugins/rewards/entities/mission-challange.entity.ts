import { VendureEntity } from "@vendure/core";
import { Column, Entity, ManyToOne } from 'typeorm';

@Entity()
export class MissionChallange extends VendureEntity {
    //** Each Channange will be have **/
    //linked mission
    // linked user
    // linked challange
}