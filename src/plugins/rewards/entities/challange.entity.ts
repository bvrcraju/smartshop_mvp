import { VendureEntity } from "@vendure/core";
import { Column, Entity, ManyToOne } from 'typeorm';

@Entity()
export class Challange extends VendureEntity {
    //event to unlock
    //action
    //linked Promotion
}